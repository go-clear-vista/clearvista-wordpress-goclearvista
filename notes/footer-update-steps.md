# Footer update (2026-09-24): Solutions column reorder + Physical Security

Only the SOLUTIONS column changes. New order: Command & Control, Physical Security, Video Conferencing,
Classroom Technologies, Council Rooms, Digital Signage, Visual Displays. Everything else is identical.

Code to paste: `notes/footer-code-paste.txt` (one line — paste as-is).

1. wp-admin → Divi/Extra → Theme Builder → Default Website Template → Global Footer → pencil (edit).
2. Hover the navy footer → click the grey gear on the **Code** module (the only module in the footer).
3. In the Content box: click inside, Ctrl+A (Cmd+A on Mac), Delete, then paste the new code.
4. Click the green ✓, then **Save** (bottom-right, or top-right purple Save), exit the builder,
   then **Save Changes** on the Theme Builder screen.
5. WP Rocket → Clear and Preload Cache.
